@model web.Models.Client

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Client</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.LastName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.LastName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.FirstName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FirstName)
        </dd>
        <dt class="col-sm-2">
    @Html.DisplayNameFor(model => model.Accesses)
</dt>
<dd class="col-sm-10">
    <table class="table">
        <tr>
            <th>Sensor</th>
        </tr>
        @foreach (var item in Model.Accesses)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Sensor.SerialNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sensor.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sensor.Location)
                </td>
            </tr>
        }
    </table>
</dd>
    </dl>

</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ClientId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
